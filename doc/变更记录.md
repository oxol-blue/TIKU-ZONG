# 变更记录

## 2026-07-14

- 后端/前端：OCS 第三方题库源支持主页、请求头、优先级、启用状态及安全自定义字段 DSL（`value/template`、`replace`、`map`、`split`、`join`）；URL 与字符串参数可替换 OCS 占位符。出于安全设计，原生 JavaScript `handler` 配置会被拒绝；无数据库迁移。

- 后端/前端：增加 API Key 主动停用接口和用户操作入口；无数据库迁移。
- 前端：管理员用户管理页新增手动发放已上架套餐的对话框，直接调用既有发放接口；无数据库迁移。
- 后端/前端：AI 模型支持编辑与单独启停；AI 兜底仅使用启用模型并保持优先级故障转移；无数据库迁移。
- 后端/前端：新增用户搜索记录、AI 搜索记录接口与页面；新增并应用 `000016_search_history.sql`。
- 后端/前端：仪表盘新增来源命中、Token、套餐消耗与错误率指标；新增并应用 `000017_call_log_sources.sql`。

## 2026-07-14

- 后端：新增 CSV/XLSX 题库导入接口和逐行导入报告，依赖 `excelize/v2`；不涉及数据库迁移。
- 前端：管理后台题库页新增 CSV/Excel 选择上传、导入统计和错误行展示。

## 2026-07-14

- 前端：按真实登录角色注册菜单和路由，管理员后台改为管理员可见、普通用户不可访问。
- 前端：OCS 配置支持在 API Key 已创建但明文未保留时由用户重新粘贴 Key 生成，不持久化该敏感值。
- 前端：统一题型字典，修正判断题值并扩展至全部需求题型；无数据库迁移和接口兼容性破坏。
- 后端：MySQL 启动探针延长至 10 秒；已将 `000012`—`000015` 应用到目标 MySQL 5.7 实例。
- 前端：登录页统一为题库调用系统品牌和邮箱登录文案，移除 koi-ui 备案/品牌遗留内容。

## 2026-07-13

- 建立从认证、计费、支付、OCS、AI 到管理后台的持续开发记录。
- 增加管理员用户、题库、订单、调用日志和 AI 答案管理。
- 增加题目/选项相似度召回、OCS 优先级和多数投票规则。
- 增加生产配置示例和订单过期自动维护任务。

## 记录规则

每次变更记录影响模块、数据库迁移、API 变化、测试结果和兼容性影响；配置行为变化必须同步 `.env.example`、部署文档和版本发布记录。
