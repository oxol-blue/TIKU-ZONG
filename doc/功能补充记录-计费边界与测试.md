# 功能补充记录：计费边界与测试

## 本次完成

- 套餐编辑和套餐上下架接口改为幂等更新：MySQL 更新相同值时不会误报资源不存在。
- 优惠券启用/停用接口改为幂等更新，并在更新前确认优惠券存在。
- 套餐创建和编辑共用类型、有效期、次数、价格、AI 次数及标记校验。
- 新增计费服务单测，覆盖非法套餐类型、时间/次数参数、负数金额、优惠券折扣和状态校验。

## 验证

- `go vet ./...` 通过。
- `go test ./...` 通过。
- `pnpm run type:check` 通过。

## 当前运行状态

本轮检查时本机没有 `5730` 或 `8088` 监听进程，因此未进行运行态 HTTP 验证。新增公告迁移仍需执行 `cd backend; go run ./cmd/migrate`，然后重启后端。
