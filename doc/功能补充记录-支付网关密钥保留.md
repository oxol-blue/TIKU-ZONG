# 功能补充记录：支付网关密钥保留

支付网关查询接口始终不会返回易支付密钥明文。为支持管理员修改网关名称、地址、商户号和启用状态，保存接口现在遵循以下规则：

- 首次创建网关必须提交 `secretKey`。
- 已存在网关的更新请求中，空 `secretKey` 会保留数据库内已加密的旧密钥。
- 提交非空 `secretKey` 才会使用主密钥重新加密并替换旧密钥。

这样前端无需为了保存非敏感配置而缓存或回显支付密钥。
