# 功能补充记录：套餐管理与用户中心

## 本次完成

- 新增用户中心页面 `/tiku/profile`：展示当前账号、角色、有效套餐、普通次数和 AI 次数。
- 新增使用帮助页面 `/tiku/help`：说明搜题、计费、API、OCS 和答案反馈规则。
- 管理后台套餐页新增全部套餐列表，支持查看上架状态并执行上架/下架。
- 管理后台优惠券区域新增优惠券列表，展示折扣、使用量、锁定量、有效期和状态。
- 新增管理员接口：
  - `GET /api/v1/admin/packages`
  - `PATCH /api/v1/admin/packages/:id/status`
  - `GET /api/v1/admin/coupons` 已接入前端列表。

## 验证记录

- `backend`: `go test ./...` 通过。
- `frontend`: `pnpm run type:check` 通过。
- `frontend`: `pnpm run build` 通过。
- 构建仍会报告依赖包的 `#__PURE__` 注释警告和主包体积提示，不影响构建退出码。

## 后续工作

- 增加公告数据表、管理员公告编辑和用户公告列表。
- 增加套餐编辑、优惠券停用和支付网关配置回显。
- 使用真实支付网关沙箱完成支付通知、退款和对账验收。
