# 功能补充记录：公告与支付配置

## 本次完成

- 新增 MySQL 迁移 `000012_announcements.sql`。
- 新增公告服务：用户查看已发布公告，管理员创建、编辑、置顶、发布和下架公告。
- 新增用户页面 `/tiku/announcements` 并加入题库中心菜单。
- 管理后台新增“公告管理”标签页。
- 新增支付网关脱敏查询接口，前端回显名称、地址、商户号和密钥是否已配置，不回显密钥。

## 验证

- `backend`: `go vet ./...` 通过。
- `backend`: `go test ./...` 通过。
- `frontend`: `pnpm run type:check` 通过。
- `frontend`: `pnpm run build` 通过。

## 注意

执行 `cd backend; go run ./cmd/migrate` 后，服务端才会加载公告表。现有运行中的后端进程需要重启后才能使用新增路由。
