# 版本发布记录

## 当前版本

分支：`main`。最近提交：`456e5b2 feat(search): add similarity and answer merge rules`。

## 已交付能力

认证与 API Key、题库导入/搜索/去重、套餐与支付、优惠券、AI 兜底、OCS 兼容、安全控制，以及管理员用户、题库、订单、调用日志和 AI 答案管理。

## 发布前检查

```text
go test ./...
go vet ./...
pnpm run type:check
pnpm run build
数据库迁移状态确认
环境变量与密钥检查
备份可恢复性检查
```

## 未纳入本版本

Redis 分布式限流、真实支付退款调用、邀请码、完整监控告警、正式验收报告和大规模相似题效果评估。
